* blog 앱을 만들었다.
- startapp으로 만들었고, bookmark처럼 생성된거다.
- settings.py에 등록.
- 이제 model을 코딩하는데, Blog앱은 Post 테이블 하나가 필요하고 테이블 뿐만 아니라
필요한 메소드도 같이 정의해줘야 한다.
장고에선 테이블이 클래스인 ORM이다. 그러므로 모델 클래스에 관련된 매소드도 같이 정의할 수 있는 것이다.

(질문) 그런데, 왜 모델에서 class 안에 그냥 변수마냥 들어가 있는걸까? 왜 self.title =  이런식으로 하지 않는걸까?

- 그 외 정말 많은 attributes들을 추가한다.

*